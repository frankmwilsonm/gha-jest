import express from "express";
import express from "express";
import express from "express";
import express from "express";
import express from "express";
import express from "express";


app.post("/newuser", (req, res) => {
    res.status(200).json();
  });

app.post("/newuser", (req, res) => {
    res.status(200).json();
  }); 

app.post("/newuser", (req, res) => {
    res.status(200).json();
  });

app.post("/newuser", (req, res) => {
    res.status(200).json();
  }); 

  
app.post("/newuser", (req, res) => {
    res.status(200).json();
  });

app.post("/newuser", (req, res) => {
    res.status(200).json();
  }); 

app.post("/newuser", (req, res) => {
    res.status(200).json();
  });

app.post("/newuser", (req, res) => {
    res.status(200).json();
  }); 

  
app.post("/newuser", (req, res) => {
    res.status(200).json();
  });

app.post("/newuser", (req, res) => {
    res.status(200).json();
  }); 

app.post("/newuser", (req, res) => {
    res.status(200).json();
  });

app.post("/newuser", (req, res) => {
    res.status(200).json();
  }); 

  import express from "express";
import express from "express";
import express from "express";
import express from "express";
import express from "express";


export default app;

export default app;

export default app;

export default app;

export default app;

export default app;

import express from "express";
import express from "express";
import express from "express";
import express from "express";
import express from "express";

import express from "express";
import express from "express";
import express from "express";
import express from "express";
import express from "express";

import express from "express";
import express from "express";
import express from "express";
import express from "express";
import express from "express";

import express from "express";
import express from "express";
import express from "express";
import express from "express";
import express from "express";

import express from "express";
import express from "express";
import express from "express";
import express from "express";
import express from "express";

import express from "express";
import express from "express";
import express from "express";
import express from "express";
import express from "express";

import express from "express";
import express from "express";
import express from "express";
import express from "express";
import express from "express";